@model IEnumerable<DAL.Person>

@{
    ViewBag.Title = "List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>List</h2>


<div ng-app="App">
    <div class="row" ng-controller="personController">
        <div class="row">
            <div class="col-md-6">
                <table class="col-md-6">
                    <tr>
                        <th style="width: 150px;">
                            <label>Title</label>
                        </th>
                        <th style="width: 150px;">
                            <label style="width: 150px;">Prenom</label>
                        </th>
                        <th>
                            <label>Nom</label>
                        </th>
                        <th></th>
                    </tr>


                    <tr ng-repeat="item in persons">
                        <td>{{item.Title}}
                        </td>
                        <td>{{item.FirstName}}
                        </td>
                        <td>{{item.LastName}}
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                        </td>
                    </tr>
                </table>
            </div>

            <div class="col-md-6">
                <div>
                    <button type="button" class="btn btn-default" data-toggle="modal" data-target="#addperson">
                        <span class="glyphicon-plus-sign" aria-hidden="true"></span>Add Person
                    </button>
                </div>
            </div>
        </div>


        @*ADD PERSON*@
        <div class="modal fade" id="addperson" tabindex="-1" role="dialog">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                        <h4 class="modal-title">Add Person</h4>
                    </div>
                    <div class="modal-body">
                        @*<div class="row">
                    <div class="col-md-6">
                        <input type="text" class="form-control" placeholder="Type" ng-model="person.PersonType"/>
                    </div>
                    <div class="col-md-6">
                         <input type="text" class="form-control" placeholder="Style Nom" ng-model="person.NameStyle"/>

                    </div>
                </div>*@

                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                        <button type="button" class="btn btn-primary">Save changes</button>
                    </div>
                </div>
                <!-- /.modal-content -->
            </div>
            <!-- /.modal-dialog -->
        </div>
        @*END ADD PERSON*@
    </div>





</div>






@section Scripts{

    @Scripts.Render("~/person-list")
    @Scripts.Render("~/Bootstrapjs")
    
}

